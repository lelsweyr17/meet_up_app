import 'package:flutter/material.dart';

class EditingTextField extends StatelessWidget {
  const EditingTextField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
